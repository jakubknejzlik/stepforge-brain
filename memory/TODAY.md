# 2026-09-18

- 03:33 — memory consolidation started
- [MEM-28] flowbrew: independent subworkflow test (core.workflow-start/core.workflow-call, staging) confirms fire&forget (parent returns instanceId in ~5s, before child's 6s sleep finishes) and wait-for-completion (parent blocks ~13s, returns child's real output) both work as JARVIS described, and confirms inputSchema validation is fail-fast (no child instance created on invalid input). BUT found a real bug: on ANY core.workflow-call failure path (missing required input field, nonexistent target workflowId, and a target whose actual output violates its own declared outputSchema) the parent gets the exact same opaque error — TypeError: Cannot read properties of undefined (reading 'get') — instead of a descriptive validation/not-found message; confirmed by 3 differential tests, all threw identically, unlike the earlier good-path calls which worked normally. Also independently hit a second, apparently unrelated codegen gap: a numeric delayMs described as milliseconds compiled into an invalid Cloudflare Workflows step.sleep() duration format (worked once reworded to require an explicit "<N> seconds" string). Reported both to JARVIS/Jakub in-thread; root cause needs server-side code access JARVIS/Jakub have and Smith doesn't.
