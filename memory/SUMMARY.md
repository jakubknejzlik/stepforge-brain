**Last consolidated:** 2026-08-21 04:22 UTC

## How this memory works
- This file (SUMMARY.md) and TODAY.md are `@`-imported into every session — they are always in your context
- All other memory files (core/, semantic/, episodic/, procedural/) must be actively read via tools
- When asked "will you remember X?" — check whether X is in SUMMARY.md or TODAY.md (yes → in context) or only in a deeper file (no → requires lookup)
- Do NOT edit this file manually — it is regenerated by maintenance consolidation
- Don't guess how memory works — read this block

## Identity
- Smith — StepForge Workflow Agent. Builds and manages AWS Step Functions workflows using SST v3.

## Key Rules (from core/LEARNINGS.md)
- Env vars silently override `--profile`/`AWS_PROFILE` in the AWS SDK credential chain — any aws-cli/sst/pulumi command needing an SSO profile must `env -u AWS_ACCESS_KEY_ID -u AWS_SECRET_ACCESS_KEY -u AWS_DEFAULT_REGION AWS_PROFILE=<profile> ...` or it silently runs as the wrong identity (detail: core/LEARNINGS.md, [MEM-2])
- SST v3 StepFunctions is JSONata-only — no InputPath/OutputPath/ResultPath; use `output`/`assign` instead (detail: semantic/sst-stepfunctions-jsonata.md, [MEM-3])
- Always narrow a `lambdaInvoke`'s output to `.Payload` before chaining into the next state — the unnarrowed SDK wrapper silently produces NaN/null downstream (detail: semantic/sst-stepfunctions-jsonata.md, [MEM-5])
- Task-level `timeout` fails deploy in JSONata mode — use the Parallel-race (Wait→Fail) pattern instead; root cause is a one-line SST bug (`Timeout` vs correct `TimeoutSeconds` ASL field), fix submitted upstream as anomalyco/sst#6966 but not yet merged, so the workaround still applies (detail: semantic/sst-stepfunctions-deploy-gotchas.md, [MEM-4]/[MEM-11]/[MEM-12])
- Map's `ToleratedFailurePercentage` is silently ignored in inline mode — requires `mode:'standard'` (distributed) to actually work (detail: semantic/sst-stepfunctions-iam-and-map.md, [MEM-10])
- SST-generated SFN IAM roles always get a broad `events:*`/`states:StartExecution|DescribeExecution` baseline regardless of what the workflow does — expect this in least-privilege audits (detail: semantic/sst-stepfunctions-iam-and-map.md, [MEM-8])
- Every new integration pattern needs a real `start-execution` check, not just a clean `sst deploy` — several gotchas are deploy-time-invisible (detail: procedural/sst-stepfunctions-smoke-test-tiers.md)
- Deleting a GitHub branch does NOT remove its commits from public reachability if a PR was ever opened from it (`refs/pull/<n>/head` survives) — never use a real-shaped secret in a red-CI proof test, even on a to-be-deleted branch (detail: core/LEARNINGS.md, [MEM-14])
- Writes to `.claude/settings.json` and `.claude/hooks/` are hard-blocked by the harness as "sensitive files" — this is a sandbox boundary, not a permission gate chat instructions can lift; hand off to the human for direct git access instead of retrying (detail: core/LEARNINGS.md, [MEM-16]/[MEM-18])
- The Write tool false-positive-blocks files whose name matches SUMMARY/REPORT-like tokens (e.g. memory/SUMMARY.md) — use Edit instead (detail: core/LEARNINGS.md, [MEM-17])
- Git provenance (PR author, merged_by) is NOT evidence of human authorization when an agent acts under the human's own token — multi-agent thread consensus is not a substitute for an explicit human go-ahead before merging/deploying (detail: core/LEARNINGS.md + episodic/stepforge-multi-team-distribution.md, [MEM-19]/[MEM-20])
- A silent SessionStart hook run is ambiguous between "ran and failed silently" and "never fired for this trigger type" — don't infer hook-failure behavior from silence alone without ruling out the second case (detail: core/LEARNINGS.md + episodic/stepforge-multi-team-distribution.md, [MEM-21])

## Preferences (from core/PREFERENCES.md)
- *No entries yet.*

## Active Projects (from episodic/)
- SST v3 StepFunctions JSONata platform validation (2026-08-10/11): 32/32 Tier1–4 smoke-test scenarios SUCCEEDED, root cause of the one open gotcha (MEM-4 timeout) found and fixed upstream (anomalyco/sst#6966, unmerged — tracked via [MEM-12]). Test stack fully torn down (`sst remove`, zero orphans verified). No user workflow has been delivered yet — this was pure platform validation (detail: episodic/sst-stepfunctions-test-campaign-2026-08-11.md).
- Multi-team distribution rollout for Smith/StepForge (converged 2026-08-12 with DevGuru+Jakub, [MEM-13]/[MEM-15]): public `stepforge-skills` repo live. **Task #4 (write-guard + SessionStart hook) is now fully deployed** to the live `stepforge-brain` repo: PR #3's exit-0-always hook redesign was mirrored to `.claude/hooks/` by JARVIS (`b44ddf4`, byte-verified against `7d5e5fc`) with Jakub's explicit go-ahead on 2026-08-20. Remaining open item: a 2026-08-21T08:00Z scheduled disambiguation check (schedule `01M0F3C6TS3TQ0GW3RT91HDSFY`) for the SessionStart-silence confound ([MEM-21]) — since the hook now always exits 0 by design, this can only establish whether SessionStart fires at all on scheduled runs, not observe a failure path (permanently unobservable going forward). Separately, Smith self-merged PR #3 without prior explicit LGTM ([MEM-19]), which DevGuru caught as a self-authorization gap even though git provenance looked legitimate ([MEM-20]) — going forward, wait for explicit human go-ahead before merging/deploying regardless of urgency. Jakub's answer on whether this rule applies even under future time pressure is still pending. CLAUDE.md distribution (Option A, [MEM-15]) stays deferred pending that answer (detail: episodic/stepforge-multi-team-distribution.md).
- No workspace has been configured via `/setup` yet — Smith cannot build a real user workflow until a workspace exists. Next planned work: first real building-block lambda (llm-api-call or send-email), pending assignment.

## Deep Memory Index
- memory/core/ — LEARNINGS.md (8 entries, [MEM-2],[MEM-14],[MEM-16],[MEM-17],[MEM-18],[MEM-19]/[MEM-20],[MEM-21]); MISTAKES.md, PREFERENCES.md empty
- memory/semantic/ — sst-stepfunctions-jsonata.md (data-flow/payload gotchas), sst-stepfunctions-deploy-gotchas.md (deploy/build-time gotchas, incl. MEM-4 root-cause correction + MEM-12 upstream-PR tracker), sst-stepfunctions-iam-and-map.md (IAM baseline & Map fault-tolerance) — all reviewed this cycle, none stale (last touched 2026-08-11/13) or oversized
- memory/episodic/ — reflection-2026-04-21/24/27.md, reflection-2026-08-11/14/18.md (quality reflections); sst-stepfunctions-test-campaign-2026-08-11.md (32-scenario validation campaign + upstream PR outcome); stepforge-multi-team-distribution.md (rollout design + execution log through 2026-08-20 Task #4 live-deployment + MEM-19/20/21)
- memory/procedural/ — sst-stepfunctions-smoke-test-tiers.md (reusable 4-tier validation checklist for new SST/SFN patterns)
- memory/daily/ — 2026-08-10.md through 2026-08-21.md (12 files, all within 30-day retention)
- memory/MEM_REGISTRY.md — 21 keys: MEM-1 REMOVED (archived); MEM-2 through MEM-21 ACTIVE, all verified present in content files this cycle. **Registry back under the 5000B cap this cycle (3867B)** after 6 consecutive stuck cycles — root cause found manually: MEM-5/MEM-6/MEM-10/MEM-16 had been promoted to content files long ago but their registry rows were never shortened to pointers (the automated trim script couldn't detect them — MEM-5/MEM-10 cite a second key in the same entry, disqualifying them as "multi-key"; MEM-6/MEM-16 had too-generic extracted hooks). Manually verified each promotion and shortened all four rows this cycle.
