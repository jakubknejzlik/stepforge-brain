# 2026-08-10

- [MEM-1] blocker: AWS creds in env (jarvis-test-s3-writer, account 693805235952) have zero usable permissions (AccessDenied on cloudformation:ListStacks, states:ListStateMachines, s3:ListAllMyBuckets, all iam:List*). Cannot bootstrap/deploy SST dummy SFN state machine with this identity. Need either real SSO details (start URL/account/role) for punk-mode SSO, or a scoped IAM user with CloudFormation+IAM+StepFunctions+S3+SSM perms for SST deploy.
- [MEM-2] lesson: env has stray AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY (jarvis-test-s3-writer, no perms) that silently override --profile/AWS_PROFILE in the AWS SDK credential chain (env vars win over profile files). Any aws-cli/sst/pulumi command using an SSO profile must run with 'env -u AWS_ACCESS_KEY_ID -u AWS_SECRET_ACCESS_KEY -u AWS_DEFAULT_REGION AWS_PROFILE=<profile> ...' or it silently deploys/queries as the wrong identity and fails with confusing AccessDenied errors that look like the new profile lacks permissions.
- [23:31] test: Tier1 #1 DynamicWait — wait.time as JSONata '{% $states.input.waitSeconds %}' works, execution took 2.08s for waitSeconds=2. SUCCEEDED, output {waited:true}.
- [23:31] test: Tier1 #2 Choice — 4 executions (status a/b/c/z), all matched correct branch incl. default via .otherwise(). JSONata equality '=' (not '===') used in .when() conditions.
- [23:32] test: Tier1 #3 Fail — Fail state with error/cause args → execution status FAILED, top-level 'error'/'cause' fields on describe-execution match exactly. Also FAILED executions are REDRIVABLE (unlike SUCCEEDED which are NOT_REDRIVABLE).
- [23:32] test: Tier1 #4 ParallelJoin — 2 Pass branches joined via output referencing $states.result[0]/[1] → sum=30 correct. Tier1 #5 MapStatic — inline items:[1..5], processor Pass with output '{% $states.input * 2 %}' → [2,4,6,8,10]. Confirms Map passes each item as $states.input to the processor branch.
- [23:33] test: Tier1 #8 Intrinsics — JSONata built-ins ($uppercase, $sum, $split, $map, $substring, $join, string concat via &) all work directly in 'output' expressions: name→'SMITH AGENT', numbers sum=15, initials='sa' via $map+function($w){$substring($w,0,1)}. All 8 Tier1 scenarios (1-8) now SUCCEEDED/verified as expected.
- [MEM-3] gotcha: SST v3's sst.aws.StepFunctions component is JSONata-only — every state serializes with QueryLanguage:'JSONata' (see .sst/platform/src/components/aws/step-functions/state.ts toJSON()). There is NO InputPath/OutputPath/ResultPath/Parameters (legacy JSONPath-era ASL fields) exposed anywhere in the fluent API (checked state.ts, pass.ts, task.ts, map.ts, parallel.ts, choice.ts, wait.ts, fail.ts, succeed.ts). The JSONata equivalents are: 'output' (reshapes/selects data flowing to next state, ~InputPath+OutputPath combined) and 'assign' (stores workflow variables accessible via $varName in later states, ~ResultPath analogue for keeping data around without threading it through output). Confirmed working via Tier1 scenario 7 (InputOutputFiltering): assign:{originalInput:'{% $states.input %}'} in one Pass state was readable as $originalInput.meta two states later even after output narrowed the payload.
- [MEM-4] gotcha: SST v3 JSONata-mode StepFunctions component — ANY explicit Task-level 'timeout' arg (any integration: response/token) fails deploy with AWS provider error 'ERROR (SCHEMA_VALIDATION_FAILED): Field Timeout is not supported: provider=aws@7.20.0'. SST's task.ts unconditionally serializes timeout into ASL 'Timeout' field but this provider version rejects it for JSONata-mode state machines entirely — genuine AWS/JSONata gap, not SST bug. Workaround used for Tier2 scenarios 12 and 18a: Parallel-race pattern (Wait N sec -> Fail state, racing the slow/non-callback branch) instead of the built-in timeout field.
- [23:41] test: Tier2 #9 Echo — lambdaInvoke SUCCEEDED, echoed input correct in Payload.echoed. Note: default output is the FULL raw Lambda invoke SDK response (Payload/SdkHttpMetadata/StatusCode/etc), not narrowed to just Payload — need explicit 'output' JSONata (e.g. '{% $states.result.Payload %}') to get a clean value downstream.
- [23:41] test: Tier2 #10 Retry — flaky lambda fails twice then succeeds on 3rd attempt (succeededOnAttempt:3), .retry({maxAttempts:4,interval:'1 second',backoffRate:2}) verified via history: TaskFailed at T+0.6s, retry gap ~1.05s to 2nd TaskFailed, ~2.05s gap (backoff x2) to TaskScheduled->TaskSucceeded. Backoff math confirmed correct.
- [23:41] test: Tier2 #11 Catch — AlwaysFail lambda throws, .catch(fallback) → FallbackAfterCatch Pass state, execution SUCCEEDED with output {recovered:true}. Confirms .catch() correctly reroutes on any error to fallback state.
- [23:42] test: Tier2 #12 Timeout(race workaround) — Parallel{Sleepy(6s sleep) branch, Wait 3s->Fail branch} → execution FAILED, error='Task.RaceTimeout', cause matches. Confirms Parallel-race pattern is a viable substitute for the broken native Task timeout field in JSONata mode.
- [MEM-5] gotcha: chained lambdaInvokes bug — default output is the raw SDK response wrapper (.Payload nesting, see MEM re:#9), so feeding step1's default output straight into step2's payload as $states.input silently produces wrong values (undefined arithmetic -> NaN -> JSON null) instead of an error. Caught via Tier2 #13: doubled:null instead of 22, fixed by adding output:'{% $states.result.Payload %}' on step1. Lesson: ALWAYS narrow a lambdaInvoke's output to .Payload (or specific fields) before chaining into the next state's payload — the unnarrowed wrapper is a silent-failure trap, not a hard error.
- [MEM-6] gotcha: lambdaInvoke Task payload as a bare JSONata scalar expression fails at RUNTIME (not deploy time) with States.Runtime / 'The value for the field Payload must be a STRING' — AWS's lambda:invoke integration requires the resolved Payload to be an object (serialized to JSON), not a bare number/string. Caught via Tier2 #14 Map: processor payload:'{% $states.input %}' over items:[1..6] (each item a plain number) failed every iteration. Fix: always wrap scalar payloads in an object, e.g. payload:{value:'{% $states.input %}'}. Lesson: this class of bug is INVISIBLE at 'sst deploy' time — only surfaces when you actually run an execution, reinforcing why every scenario needs a real start-execution check, not just successful deploy.
- [23:44] test: Tier2 #14 MapLambda — after payload fix, execution SUCCEEDED. All 6 items [1..6] doubled correctly to [2,4,6,8,10,12] via per-item lambdaInvoke (MaxConcurrency:2). Same wrap-in-object payload fix as logged in MEM-6.
- [23:45] test: Tier2 #15 DistributedMap (mode:standard) — 12 items processed via distributed child executions, SUCCEEDED. Note: much slower to spin up than inline Map (~15-20s vs <5s) due to child-execution orchestration overhead. Confirms mode:'standard' works but SST's MapArgs exposes no itemBatcher/ItemReader field — only proves the distributed execution mode itself, not batching.
- [23:45] test: Tier2 #16 ParallelDiff — 2 different lambdas (parallelUpper, parallelLength) in Parallel branches, joined via output referencing $states.result[0].Payload/[1].Payload. Input 'hello world' -> upper:'HELLO WORLD', length:11, matches:true (uppercase length == length branch result). Confirms cross-branch JSONata result indexing works for heterogeneous lambda outputs.
- [23:46] test: Tier2 #17 TaskToken(auto) — lambdaInvoke integration:'token', lambda receives $states.context.Task.Token in payload, calls SendTaskSuccess itself. Execution SUCCEEDED, output {approvedBy:'tokenAuto-lambda'} = the SendTaskSuccess output payload. Confirms waitForTaskToken -> lambda -> SendTaskSuccess round trip works end to end.
- [23:46] test: Tier2 #18a TaskTokenTimeout(race) — Parallel{tokenTimeout (waitForTaskToken, lambda never calls back) branch, Wait 5s->Fail branch} → execution FAILED, error='TaskToken.RaceTimeout'. Confirms same Parallel-race workaround (from #12) also covers the token-integration timeout case, since native timeout field is unusable for waitForTaskToken tasks too (MEM-4).
- [23:46] test: Tier2 #18b TaskTokenFail(explicit) — lambda receives token, calls SendTaskFailure(error='ExplicitTaskTokenFailure',cause=...) directly. Execution FAILED, top-level error/cause match exactly. Confirms explicit-failure callback path (as opposed to no-callback timeout path in #18a). All 10 Tier2 scenarios (9-18, with 18 split a/b) now SUCCEEDED/FAILED-as-expected and verified.
- [23:56] test: Tier3 #19 SqsSendMessage — sqsSendMessage task SUCCEEDED, output is raw SDK SendMessage response wrapper (MD5OfMessageBody/MessageId/SdkHttpMetadata, same shape pattern as lambdaInvoke per MEM-5). Verified via sqs receive-message: Body='hello-from-tier3-test-19' matches input exactly, MD5 matches.
- [23:57] test: Tier3 #20 SnsPublish — snsPublish task SUCCEEDED, verified via subscriber queue: SNS-wrapped envelope Message field='hello-from-tier3-test-20' matches input exactly.
- [23:57] test: Tier3 #21 EventBridgePutEvents — eventBridgePutEvents task SUCCEEDED, verified via bus.subscribeQueue with source-filter pattern: event detail.foo='hello-from-tier3-test-21' matches input, resources[] correctly references the emitting execution ARN.
- [23:57] test: Tier3 #22 EventBridge schedule->SFN — raw @pulumi/aws EventRule+EventTarget+IAM role (no SST component wires schedule directly to a state machine, only to function/ECS task) auto-triggered Tier3ScheduleTarget every 1 min since deploy, 4/4 executions SUCCEEDED, output {tickedAt} correct, input is standard EventBridge Scheduled Event envelope.
- [23:57] test: Tier3 #23 DynamoPutGet — task() escape-hatch (no SST DynamoDB helper exists, unlike sqs/sns) with dynamodb:putItem->getItem chained via assign:{itemId} pattern (MEM re: Tier1#7). SUCCEEDED, getItem output narrowed to {% $states.result.Item %} returned {pk:{S:'tier3-test-23'},value:{S:'hello-from-tier3-test-23'}} exactly matching what was put.
- [23:58] test: Tier3 #24 S3 write/read via linked Bucket — 2 lambdaInvoke (s3Write.next(s3Read)), both output-narrowed to $states.result.Payload (per MEM-5). SUCCEEDED end to end, output {key:'tier3-test-24.txt',body:'hello-from-tier3-test-24'} matches what was written. Resource.<BucketName>.name link pattern confirmed working.
- [23:58] test: Tier3 #25 Nested SFN — parent uses arn:aws:states:::states:startExecution.sync:2 (the '.sync:2' suffix parses child output as JSON vs raw string for plain .sync) + extra events:PutTargets/PutRule/DescribeRule perms (AWS polls completion via an internal EventBridge rule). Parent SUCCEEDED, output={childRan:true} correctly parsed as object, child execution independently confirmed SUCCEEDED in its own state machine's execution list.
- [23:58] test: Tier3 #26 Express vs Standard — Standard: start-execution (async) + describe-execution (persistent history) works normally, SUCCEEDED {workflowType:'standard',ok:true}. Express: must use start-sync-execution (synchronous, output returned inline in the start call itself, billingDetails incl. billedMemoryUsedInMB/billedDurationInMilliseconds), and describe-execution on an express ARN hard-fails with InvalidArn ('Resource type not valid in this context: express') — Express has no persistent execution record to query. All 8 Tier3 scenarios (19-26) now verified SUCCEEDED.
- [MEM-7] gotcha: SST v3 Resource linking (import {Resource} from 'sst' in a lambda handler) requires the 'sst' npm package itself as a runtime dependency in package.json — it's NOT auto-provided just because sst-env.d.ts declares the module, and NOT the same as the sst CLI (bunx/bun x sst, resolved separately e.g. from npx cache). Deploy fails at build time with esbuild error 'Could not resolve "sst"' pointing at the handler file. Fix: bun add sst@<version matching CLI, e.g. 4.17.1>. First hit in Tier3 scenario 24 (S3Bucket linked function) — Tier1/Tier2 handlers never needed Resource linking so this never surfaced before.
