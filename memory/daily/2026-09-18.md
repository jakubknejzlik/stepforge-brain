# 2026-09-18

- [03:30] maintenance: consolidation session
- 03:33 — memory consolidation started
- [MEM-28] flowbrew: independent subworkflow test (core.workflow-start/core.workflow-call, staging) confirms fire&forget (parent returns instanceId in ~5s, before child's 6s sleep finishes) and wait-for-completion (parent blocks ~13s, returns child's real output) both work as JARVIS described, and confirms inputSchema validation is fail-fast (no child instance created on invalid input). BUT found a real bug: on ANY core.workflow-call failure path (missing required input field, nonexistent target workflowId, and a target whose actual output violates its own declared outputSchema) the parent gets the exact same opaque error — TypeError: Cannot read properties of undefined (reading 'get') — instead of a descriptive validation/not-found message; confirmed by 3 differential tests, all threw identically, unlike the earlier good-path calls which worked normally. Also independently hit a second, apparently unrelated codegen gap: a numeric delayMs described as milliseconds compiled into an invalid Cloudflare Workflows step.sleep() duration format (worked once reworded to require an explicit "<N> seconds" string). Reported both to JARVIS/Jakub in-thread; root cause needs server-side code access JARVIS/Jakub have and Smith doesn't.
- [MEM-29] flowbrew: while starting the serial multi-subworkflow test (~30min after MEM-28's clean results), found core.workflow-start AND core.workflow-call both now fail on staging even for a bare, trivial, valid single call (same generic TypeError: Cannot read properties of undefined (reading 'get')) — a regression from the working state confirmed minutes earlier. core plugin manifest version unchanged (1.6.0), so likely a server-side change, not a manifest redeploy. Flagged to JARVIS/Jakub in-thread as urgent before continuing serial/parallel testing, since JARVIS was running a concurrent parallel test via MCP at the same time — possible correlation, unconfirmed. Test workflows cleaned up (deleted) as usual.
- [MEM-30] flowbrew: MEM-29 scope broadened — JARVIS independently reproduced the same TypeError: Cannot read properties of undefined (reading 'get') firing a totally unrelated pre-existing staging workflow (Issue 117 repro, created 2026-09-11, no subworkflow brick at all). 3 independent firings (mine, JARVIS's earlier bg-agent run, this one) all hit the identical error — confirms this is a general staging start_workflow/runtime failure right now, not subworkflow-specific. JARVIS's wrangler tail on WorkflowStartService/core-plugin caught nothing (wrong worker — plain start_workflow is served by something else, deploy-coordinator/McpServer candidate). Agreed with JARVIS/Jakub in-thread to pause serial/parallel subworkflow testing until this is resolved. Offered to independently fire the same test against PROD (I hold FLOWBREW_API_TOKEN) if Jakub/JARVIS want a second independent prod check, decision pending.
- [MEM-31] flowbrew: MEM-29/30 regression resolved. JARVIS traced root cause via wrangler tail + Cloudflare audit log to a missing env.LOADER (Worker Loader) binding on the staging runtime worker (dynamic-workflow-host.ts:85), caused by JARVIS's own earlier lost/orphaned bg-agent running a stray manual full sst deploy --stage staging around 06:54 UTC (meant to spin up an isolated PR preview stage, hit shared staging instead) which wiped the post-deploy patch:worker-loader step's effect. JARVIS took responsibility, got Jakub's explicit go-ahead ("ano"), ran patch:worker-loader, verified via CF API + a live workflow firing. I independently re-confirmed by re-firing my own earlier core.workflow-call test workflow (parent 5a37aab5.../child b5de6b9e...) — completed cleanly in 14.56s with the correct output, no more TypeError. Staging confirmed healthy again; resuming the serial multi-subworkflow test JARVIS asked me for before the interruption.
- [MEM-32] flowbrew: serial multi-subworkflow test (post LOADER-fix) confirms core.workflow-call in a for-loop is reliable, sequential (~31s for 3x2s-delay children, not parallelized), and each call gets its own isolated child instance/id with no cross-call overwriting. Uncaught mid-sequence failure aborts the rest of the sequence (normal JS behavior); with per-call try/catch, subsequent calls proceed normally after a caught failure. BIGGER finding: re-ran the exact 3 MEM-28 bad-input/bad-output/bad-target scenarios on the now-healthy platform and got 3 distinct, specific, actionable error messages (Zod path detail, 'target unavailable in workspace/project scope', outputSchema mismatch detail) instead of the old generic TypeError — strong evidence the MEM-28/issue-#199 'opaque identical error' finding was actually caused by the concurrent LOADER-binding regression (MEM-29/30/31), not a real core.workflow-call bug; timing of the original bad-input/output/target tests lines up right after JARVIS's 06:54:46-06:55:06 stray deploy. Only remaining minor nuance: an uncaught error surfaces a terser wrapper via read_instance than the same error caught via try/catch shows (400 generic vs 422 detailed) — a UX note, not a bug. Proposed to JARVIS/Jakub in-thread to correct/narrow issue #199 since its main claim didn't reproduce on a healthy platform; awaiting their call on who edits the issue. All test workflows cleaned up as usual.
- [MEM-33] flowbrew: subworkflow testing campaign (2026-09-18) closed out. JARVIS posted MEM-32's correction to issue #199 and closed it as false positive; independently verified via gh api (state:closed, closed_at 2026-09-18T12:59:40Z, comment text matches). Final joint result with JARVIS: core.workflow-start/core.workflow-call fully functional across all tested scenarios — fire&forget, wait+output propagation, inputSchema fail-fast, outputSchema violation propagation, readable errors for bad input/target/output (post LOADER-fix), N children in parallel (Promise.all, both wait and fire&forget, ~14s vs ~39s serial-equivalent confirming real parallelism, distinct non-colliding instanceIds), N children serially (for-loop, ~31s = sum of delays, distinct instances, uncaught mid-sequence failure aborts the rest, caught failure lets it continue). Remaining open items: delayMs(ms)->step.sleep() codegen gap noted as a separate minor item, not yet filed; the read_instance terse-wrapper-on-uncaught-error UX note landed as a follow-up idea in the #199 closing comment, not its own issue.
