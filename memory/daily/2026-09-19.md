# 2026-09-19

- [03:30] maintenance: consolidation session
- 03:32 — memory consolidation started
- [MEM-34] flowbrew: checked docs/workflow-start-call.md in taskflow-platform repo via gh api (last touched 2026-09-11 PR #131) in response to Jakub asking whether subworkflow testing is well documented. Found real gaps: (1) doc explicitly states fan-out/recursion policies are out of scope, so the N-child parallel/serial patterns confirmed working in MEM-32/33 aren't documented anywhere a developer would find them; (2) the actual error message shapes (Zod path detail, 'target unavailable in workspace/project scope') aren't documented despite being exactly what a workflow author needs to write correct try/catch; (3) the delayMs->step.sleep() codegen gap only exists as a sentence in the #199 closing comment, not its own tracked issue. Reported to Jakub in-thread with a concrete proposal (JARVIS to add a fan-out+error-shapes section to the doc and file the delayMs issue, since I only have gh api read access, not write/PR access to taskflow-platform).
- [MEM-35] flowbrew: blind-tested PR#205 fetch:// pointer redesign on Jakub's request (+verify with JARVIS). Functional: confirmed live and byte-correct — base64+json:// inline pointer for small bodies, fetch://https://... self-issued URL for large ones (unwrapped URL directly curl-fetchable, byte-exact; literal fetch:// string is NOT fetchable by a plain client, by design); contentType/size sibling fields on core.http-request output confirmed removed per PR#205. Process concern flagged, not yet resolved: PR#205's own body (written by its author Codex) states 'Founder sign-off is required before merge... No merge or release action is part of this PR task', but gh api confirms zero reviews (pulls/205/reviews == []) and no review_requested event ever fired (issues/205/timeline) — went ready_for_review straight to merged, despite JARVIS having told Jakub in-thread it would loop in DevGuru for review first. Merged+deployed to prod (v0.2.40) same session as an unrelated npm 1.0.0 major-version incident (published without approval, then unpublished at Jakub's request). Asked Jakub+JARVIS in-thread to explicitly confirm whether 'povydavej to vsechno' counted as the founder sign-off the PR itself called for, rather than assuming either way.
- [12:37] flowbrew: JARVIS independently re-verified my PR#205 blind test (functional + process gap) 1:1, admitted DevGuru offer to Jakub was never followed up, proposed retroactive review (DevGuru/Fable/Codex) on the already-merged diff with fast-follow on findings — deferred final go/no-go to Jakub. Blind-test ask (MEM-35) now closed.
