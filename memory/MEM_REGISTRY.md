# MEM Registry

| Key | Status | Created | Obsoleted | Description |
|-----|--------|---------|-----------|-------------|
| MEM-1 | REMOVED | 2026-08-10 | 2026-08-11 | blocker: AWS creds in env (jarvis-test-s3-writer, account 693805235952) had zero usable permissions, blocking SST dummy SFN deploy. Superseded same-day by knedlopark SSO setup; root-cause lesson preserved in [MEM-2]. Never promoted to a content file (no [MEM-1:obsolete] tag existed to remove) — visible one full cycle as OBSOLETE, now REMOVED per lifecycle. |
| MEM-2 | ACTIVE | 2026-08-10 | core/LEARNINGS.md:3 | see core/LEARNINGS.md — [MEM-2] The environment can carry stray AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY (e.g. |
| MEM-3 | ACTIVE | 2026-08-10 | semantic/sst-stepfunctions-jsonata.md:6 | see semantic/sst-stepfunctions-jsonata.md — [MEM-3] SST v3's sst.aws.StepFunctions component is JSONata-only — |
| MEM-4 | ACTIVE | 2026-08-10 | semantic/sst-stepfunctions-deploy-gotchas.md:6 | see semantic/sst-stepfunctions-deploy-gotchas.md — Workaround: Parallel-race pattern |
| MEM-5 | ACTIVE | 2026-08-10 | — | gotcha: chained lambdaInvokes bug — default output is the raw SDK response wrapper (.Payload nesting, see MEM re:#9), so feeding step1's default output straight into step2's payload as $states.input silently produces wrong values (undefined arithmetic -> NaN -> JSON null) instead of an error. Caught via Tier2 #13: doubled:null instead of 22, fixed by adding output:'{% $states.result.Payload %}' on step1. Lesson: ALWAYS narrow a lambdaInvoke's output to .Payload (or specific fields) before chaining into the next state's payload — the unnarrowed wrapper is a silent-failure trap, not a hard error. |
| MEM-6 | ACTIVE | 2026-08-10 | — | gotcha: lambdaInvoke Task payload as a bare JSONata scalar expression fails at RUNTIME (not deploy time) with States.Runtime / 'The value for the field Payload must be a STRING' — AWS's lambda:invoke integration requires the resolved Payload to be an object (serialized to JSON), not a bare number/string. Caught via Tier2 #14 Map: processor payload:'{% $states.input %}' over items:[1..6] (each item a plain number) failed every iteration. Fix: always wrap scalar payloads in an object, e.g. payload:{value:'{% $states.input %}'}. Lesson: this class of bug is INVISIBLE at 'sst deploy' time — only surfaces when you actually run an execution, reinforcing why every scenario needs a real start-execution check, not just successful deploy. |
| MEM-7 | ACTIVE | 2026-08-10 | semantic/sst-stepfunctions-deploy-gotchas.md:7 | see semantic/sst-stepfunctions-deploy-gotchas.md — Fix: `bun add sst@<version matching CLI>` |
| MEM-8 | ACTIVE | 2026-08-11 | semantic/sst-stepfunctions-iam-and-map.md:6 | see semantic/sst-stepfunctions-iam-and-map.md — worth flagging for IAM audits |
| MEM-9 | ACTIVE | 2026-08-11 | semantic/sst-stepfunctions-deploy-gotchas.md:8 | see semantic/sst-stepfunctions-deploy-gotchas.md — Workaround: |
| MEM-10 | ACTIVE | 2026-08-11 | — | gotcha: ToleratedFailurePercentage/ToleratedFailureCount on a Map state is SILENTLY IGNORED by AWS Step Functions in INLINE mode — confirmed empirically: injected ToleratedFailurePercentage:50 via transform.stateMachine (MEM re: X-Ray), verified field WAS present in the deployed ASL via describe-state-machine, ran 4 items with 1 intentional failure (25% failure rate, well under the 50% tolerance) — execution still FAILED outright (MapStateFailed -> ExecutionFailed), tolerance had zero effect. Fix: Map must use mode:'standard' (DISTRIBUTED processing, same mode as Tier2 #15 DistributedMap) for ToleratedFailurePercentage to actually take effect. Re-ran identical scenario with mode:'standard' -> SUCCEEDED, output array has 3 successful item results + 1 {Status:FAILED,Error,Cause} entry inline instead of aborting. Lesson: AWS's fault-tolerance features for Map iterations are a DISTRIBUTED-mode-only feature, not an INLINE Map feature — this is an AWS platform restriction, not an SST gap, but SST's Map component gives no hint/validation that the field would be a no-op in inline mode (and doesn't expose the field at all, see MEM-9 transform workaround). |
| MEM-11 | ACTIVE | 2026-08-11 | semantic/sst-stepfunctions-deploy-gotchas.md:7 | see semantic/sst-stepfunctions-deploy-gotchas.md — correction to MEM-4's root cause, verified empirically, upstream PR anomalyco/sst#6966 |
| MEM-12 | ACTIVE | 2026-08-12 | — | reference: track upstream PR anomalyco/sst#6966 (Timeout->TimeoutSeconds fix for JSONata-mode StepFunctions Task timeout, filed 2026-08-11 from fork jakubknejzlik/sst). Once merged/released, the Parallel-race workaround (semantic/sst-stepfunctions-deploy-gotchas.md, MEM-4/MEM-11) is no longer needed for Task-level timeout — check PR status before reaching for the workaround on new SST workflow work, and update the gotcha doc once it lands. |
| MEM-13 | ACTIVE | 2026-08-12 | — | project: multi-team distribution design for Smith/StepForge, converged with DevGuru + Jakub (2026-08-12). Topology: (1) new CLEAN-history public repo containing only skills/ + generic docs, submoduled read-only into each team's own repo; (2) team repo = SST code + durable team docs (architecture/decisions/conventions) together, split by write-frequency+audience not brain-vs-workspace; (3) agent's operational scratchpad (daily logs, episodic, MEM_REGISTRY, reports) stays local to the agent, never goes into any shared/reviewed repo; (4) scope is Smith/StepForge instances only, NOT a universal brain across all platform agent types (that's the separate base-brain tier). Public-repo gate needs TWO layers: shape-based grep as mechanical pre-push/CI gate (account IDs, ARNs, access keys, emails, credential URLs — must fail loudly, non-zero exit) AND a human review step per-PR for 'does this reveal too much internal detail' (no grep catches judgment calls) — write this into the new repo's CONTRIBUTING.md when it's created, not just check once at founding. Still open: Jakub's decision on license + external-PR acceptance policy for the new public repo. Also found and need to fix before any split: eu-central-1 hardcoded (not placeholder'd) in aws-auth/SKILL.md and setup/SKILL.md — inconsistent with the file's own YOUR_ACCOUNT_ID/YOUR_ORG placeholder convention. |
| MEM-14 | ACTIVE | 2026-08-12 | — | correction: deleting a git branch on GitHub does NOT delete its commits from public reachability if a PR was ever opened from it — the commits stay accessible via refs/pull/<n>/head even after the branch is deleted and the PR is closed unmerged. Confirmed by DevGuru independently re-fetching the fake AKIA test string from stepforge-skills PR #1 after I'd already deleted the branch. Consequence: any future red-CI proof test must use an obviously-synthetic value only, never a real-shaped live secret — closing the PR and deleting the branch is not a cleanup mechanism, treat anything pushed to a public repo as permanently public regardless of branch/PR state. Also: the check-secrets.sh gate only scans git ls-files at HEAD, not history or PR refs — documented as a known scope limit in stepforge-skills CONTRIBUTING.md. |
